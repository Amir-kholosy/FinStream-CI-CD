pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Repository Loaded'
            }
        }

        stage('Build') {
            steps {
                echo 'Building FinStream UI'
            }
        }

        stage('Test') {
            steps {
                echo 'Running Tests'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying Application'
            }
        }

    }
}